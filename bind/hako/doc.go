// Package hako is the gomobile binding layer for the mihomo core.
//
// Nested Go module: mihomo sources live at the repository root and
// are wired in via a local replace directive; this package must stay within
// gomobile's exportable type subset (no maps, no struct slices, no
// (string, error) returns).
package hako
